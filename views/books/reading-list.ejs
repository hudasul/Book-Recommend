<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reading List</title>
    <link rel="stylesheet" href="/books-style/reading-list.css">
  </head>
  <body>
    <header>
      <nav><%- include('../partials/navbar') %>
      <a href="/books"  class="all-books-link">All books</a>
      </nav>
    <h1>My Reading List</h1>
   </header>

   <div class="container">
    <% if (readingList.length === 0) { %>
    <p>No books added yet!</p>
    <% } else { %> <% readingList.forEach(book => { %>
      <div class="book"> 
    <h2><%= book.title %></h2>
    <p>Author: <%= book.author %></p>
    <p>Rating: <%= book.rating%>/5</p>
    <p>Genre: <%= book.genre %></p>
    <div class="forward-to-another-page">
    <a href="/books/<%= book._id%>" class="details">View Details</a>
  <form action="/books/<%= book._id %>/reading-list?_method=DELETE" method="POST">
  <button type="submit">Remove from Reading List</button>
  </form>
    </div>
    </div>
    <% }) %> <% } %>
    </div>
  </body>
</html>
